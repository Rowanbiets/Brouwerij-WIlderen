
  
  document.addEventListener('swup:contentReplaced', () => {
    console.error('swup:contentReplaced');
    console.error('swup:contentReplaced');
    // applySavedLanguage();
  });

  console.error('swup:contentReplaced');

